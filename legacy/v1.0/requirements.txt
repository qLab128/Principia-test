# Principia v1 intentionally keeps the runtime on the Python standard library.
# Requires Python 3.9 or newer. Python 3.12 is recommended.
# Optional: install pypdf separately only if you want PDF-specific tests or ingestion.
